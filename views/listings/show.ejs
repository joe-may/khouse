<%-include('../partials/header') %>
<div class="container">

<section id="show-page">
    <div class="container">
        <form class="new-form" action="/listings" method="POST">
          <label>date</label>
              <br>
              <%= listing.date %>
              <br>
              <br>
          <label>title</label>
             <br>
            <%= listing.title %>
             <br>
             <br>
             <label>budget</label>
              <br>
              <%= listing.budget %>
              <br>
              <br>
          <label>location</label>
             <br>
            <%= listing.location %>
             <br>
             <br>
             <label>name</label>
             <br>
            <%= listing.name %>
             <br>
             <br>
             <label>contact</label>
             <br>
            <%= listing.contact %>
             <br>
             <br>
          
        </form>

        </div>
        <h2>Comments</h2>
        <ul>
            <% listing.comments.forEach( (c) => { %>

                  <li><%= c.comment %></li>
                  <li><%= c.user_name %></li>
                 
              <% }); %>
        </ul>
        <form action="/listings/comments/<%= listing._id %>" method="POST" autocomplete="off">
            <input type="text" name="comment">
            <button type="submit">Enter a comment</button>
          </form>
          <table id="list">
            <thead>
              <tr>
                <th>Name</th>
                <th>Comment</th>
                
              </tr>
            </thead>
            <tbody>
                <% listing.comments.forEach( (c) => { %>
               
                <tr>
                  <td><%= c.user_name %></td>
                  <td><%= c.comment %></td>
        
                    
                    
                  </td>
                </tr>
                
              <% }); %>
            </tbody>
          </table>
</div>

<%- include('../partials/footer') %>